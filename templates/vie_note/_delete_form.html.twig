<form method="post" action="{{ path('app_vie_note_delete', {'id': vie_note.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vie_note.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
