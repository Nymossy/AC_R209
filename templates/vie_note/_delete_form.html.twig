<form method="post" action="{{ path('app_vie_note_delete', {'id': vie_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vie_note.id) }}">
    <button class="btn">Delete</button>
</form>
