{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
        <button type="submit" class="btn btn-outline-success">
            <i class="fas fa-save me-1"></i>{{ button_label|default('Sauvegarder') }}
        </button>
    </div>
{{ form_end(form) }}
