{% extends 'base.html.twig' %}

{% block title %}Nouveau tag{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-5">Créer un nouveau tag</h1>
            <p class="text-muted">Ajoutez un nouveau tag pour organiser vos notes</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Formulaire d'ajout</h5>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        {{ form_row(form.nom) }}
                        
                        <div class="mb-3">
                            {{ form_label(form.couleur) }}
                            <div class="input-group">
                                {{ form_widget(form.couleur, {'attr': {'class': 'form-control color-picker'}}) }}
                                <span class="input-group-text">
                                    <div id="color-preview" style="width: 24px; height: 24px; border-radius: 50%; background-color: #17a2b8;"></div>
                                </span>
                            </div>
                            <small class="form-text text-muted">Choisissez une couleur pour ce tag</small>
                            {{ form_errors(form.couleur) }}
                        </div>
                        
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <button type="submit" class="btn btn-outline-success">
                                <i class="fas fa-save me-1"></i>Sauvegarder
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
                <div class="card-footer text-end">
                    <a href="{{ path('app_tag_index') }}" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left me-1"></i> Retour à la liste
                    </a>
                </div>
            </div>
        </div>
        
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Instructions</h5>
                </div>
                <div class="card-body bg-warning bg-opacity-10">
                    <p><i class="fas fa-info-circle text-warning me-1"></i> Les tags permettent de catégoriser vos notes.</p>
                    <p><i class="fas fa-info-circle text-warning me-1"></i> Utilisez des noms courts et descriptifs.</p>
                    <p><i class="fas fa-info-circle text-warning me-1"></i> Exemples : "Urgent", "Personnel", "Travail", "Projet".</p>
                    <p><i class="fas fa-info-circle text-warning me-1"></i> Évitez les doublons pour une meilleure organisation.</p>
                </div>
            </div>
            
            <div class="card mb-4">
                <div class="card-header bg-light">
                    <h5 class="mb-0">Astuces</h5>
                </div>
                <div class="card-body">
                    <p><i class="fas fa-lightbulb text-primary me-1"></i> Les tags apparaîtront comme des badges colorés dans vos notes.</p>
                    <p><i class="fas fa-lightbulb text-primary me-1"></i> Vous pourrez filtrer vos notes par tag pour les retrouver rapidement.</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
